import 'package:flutter/material.dart';

class H extends Text {
  const H(String text, {Key? key})
      : super(
          key: key,
          text,
          textAlign: TextAlign.center,
        );
}
